04/05/85 wakeup_volume_dump

Syntax as a command:  wakeup_volume_dump


Function:  restarts the incremental volume dumper.  It is used when the
alarm clock timer fails, and when an additional pass of the dump is
needed (for example, before a shutdown).
