02/25/85 mcs_version

Syntax as a command:  mcs_version {fnp_tag}


Function:  prints the version of the core image most recently loaded
into the specified FNP.


Arguments:
fnp_tag
   where fnp_tag is the identifier of the FNP whose version is to be
   printed.  It can be a, b, c, d, e, f, g, or h.  If it is omitted, a
   is assumed.
