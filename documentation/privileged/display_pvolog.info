10/3/89  display_pvolog

Syntax as a command:  display_pvolog dvname {-control_args}


Function:  displays the data content of a physical volume log.


Arguments:
dvname
   is the dump volume name.


Control arguments:
-header, -he
   specifies to display the header (Default).
-no_header, -nhe
   specifies no header.  The default is to have one.
-pvname STR, -pv STR
   displays only data associated with the disk volume specified by STR.
-working_dir, -wd
   searches for the physical volume log in the directory [wd]>pvolog,
   rather than in the default system directory.
