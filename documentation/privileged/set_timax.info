02/27/85  set_timax, stm

Syntax as a command:  stm N


Function:  sets the maximum value that ti (time since interaction) can
assume for an interactive process.


Arguments:
N
   is the number of seconds, in decimal, to which timax is to be set.
   A value less than or equal to zero causes it use the default timax
   from tc_data$timax.


Access required:  The user must have access to both the privileged and
the highly privileged gates phcs_ and hphcs_.
