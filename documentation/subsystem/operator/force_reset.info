02/13/85 force_reset

Syntax as a command:  force_reset


Function:  forces the answering service to reset itself.  It is similar
to the reset command but does additional resetting.  If administrators
are unable to install system tables, this function can sometimes clear
the jam.  This command can be used in ring 4 only.
