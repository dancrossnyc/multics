02/21/85 unload

Syntax as a command:  unload volume_type volume_name
or
unload volume_type -control_arg


Function:  unloads volumes that are loaded and not in use.  You may
specify which inactive volume is to be unloaded or that all inactive
volumes are to be unloaded.  This command can be used in ring 4 only.


Arguments:
volume_type
   is "tape_vol".
volume_name
   is the name of the tape volume to be unloaded.


Control arguments:
-all
   specifies that all currently inactive tape volumes are to be
   unloaded.


Notes:  The unload command does not allow you to unload a volume that
is in use.
