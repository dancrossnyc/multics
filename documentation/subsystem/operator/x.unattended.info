03/15/85  unattended, unattend

Syntax as a command:  x unattend


Function:  sets up the system for unattended service.  It deletes all
the tape drives, turns on the unattended and auto-reboot flags in the
BCE flagbox, and sets the greeting message to "Unattended service".
