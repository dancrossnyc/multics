6/27/82  forum request: delete_participant, dlpt

Syntax:  dlpt user_names {-control_args}


Function:  logically removes the record of participation of a
participant in a meeting.  These attendee records may then be deleted
with the "expunge" request.


Arguments:
user_names
   are the names of the participants whose records are to be deleted.


Control arguments:
-brief, -bf
   Suppresses the "Specified switch was not changed." error message.
-long, -lg
   Prints the "Specified switch was not changed." error message if the
   reocrd for that participant had already been deleted. (Default)


Notes:  Only the chairman of the meeting may issue this request.
