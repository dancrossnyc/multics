12/11/82 forum request: new

Syntax:  new {-control_args}


Syntax as an active request:  [new {-control_args}]


Function:
Prints or returns the transaction indices of new transactions in the meeting.
New transactions are those transactions numbered higher than the user's
last_seen transaction which were not entered by that user.


Control arguments:
-include_deleted, -idl
   print or return the indices of new transactions even if they are deleted.
-only_deleted, -odl
   print or return only the indices of deleted new transactions.
-only_non_deleted, -ondl
   print or return only the indices of non-deleted new transactions.
   (Default)
