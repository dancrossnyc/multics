6/27/82  forum request: retrieve_participant, rtpt

Syntax:  rtpt user_names {-control_args}


Function:  logically retrieves the record of participation a of
participant in a meeting after it had been deleted with the 
"delete_participant" request.


Arguments:
user_names
   are the names of the participants whose records are to be retrieved.


Control arguments:
-brief, -bf
   Suppresses the "Specified switch was not changed." error message.
-long, -lg
   Prints the "Specified switch was not changed." error message if the
   reocrd for that participant had not been deleted. (Default)


Notes:  Only the chairman of the meeting may issue this request.
