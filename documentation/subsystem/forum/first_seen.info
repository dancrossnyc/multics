7/25/83 forum request:  first_seen, fs

Syntax: fs {-control_args}


Syntax as an active request:  [fs {-control_args}]


Function:
Prints or returns the transaction index of the first transaction in the
meeting that has been seen by this user.


Control arguments:
-include_deleted, -idl
   print or return the index of the first seen transaction in the meeting
   even if it is deleted.
-only_deleted, -odl
   print or return the index of the first deleted seen transaction in the
   meeting.
-only_non_deleted, -ondl
   print or return the index of the first non-deleted seen transaction in
   the meeting. (Default)
