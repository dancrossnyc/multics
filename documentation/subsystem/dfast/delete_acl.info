03/18/76   delete_acl, da

Function:  removes entries from the access control lists (ACLs) of
files.

Syntax:  delete_acl {file_name} {User_ids} {-control_args}

Arguments:
file_name  name of file.
User_ids  access control names of the form Person_id.Project_id.tag.
 If no User_ids are given, user's Person_id and Project_id are assumed.


Control arguments:
-all, -a  deletes entire ACL except entry for *.SysDaemon.*.
-brief, -bf  suppresses message "User name not on ACL."
