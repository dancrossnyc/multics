03/21/85 slave

Syntax as a command:  slave message


Function:  allows the master terminal operator to communicate with the
operator of the slave terminal by sending a message.  The slave command
is the reverse of the master command.


Arguments:
message
   is any arbitrary one-line message containing no more than 120
   characters.
