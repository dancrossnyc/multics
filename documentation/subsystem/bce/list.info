04/05/85  list, ls

Syntax as a command:  ls {star_names}


Syntax as an active function:  [ls {star_names}]


Function:  lists the names of BCE files matching a set of star names.
If no star names are specified, the list command will list the names of
all BCE files.  When used as an active function, it returns the set of
star names.  It is valid at all BCE command levels.


Arguments:
star_names
   are the names against which the names of BCE files are to be
   matched.
