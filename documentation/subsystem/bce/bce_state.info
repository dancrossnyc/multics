04/05/85  bce_state, bces

Syntax as a command:  bces


Syntax as an active function:  [bces]


Function:  prints or returns the name of the BCE state which is
currently running (early, boot, bce_crash or crash).


Notes:  This command is useful in exec_coms to diagnose cases where the
exec_com is only to run during some states and not others.
