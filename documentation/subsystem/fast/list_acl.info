01/22/76   list_acl, la

Function:  lists the access control lists (ACLs) of segments,
and directories.


Syntax:  la {path} {User_ids} {-control_args}


Arguments:
path  is the pathname of a segment or directory.
If omitted, the working directory is assumed and
no User_ids can be specified.  The star convention can
be used.

User_ids  are access control names that must be of the form
Person_id.Project_id.tag.  All ACL entries with the matching
names are listed.


Control arguments:  is brief (-bf) to suppress the message
"User name not on ACL of path."


Notes: Type "help acl_matching" for an explanation of the matching 
strategy used on User_ids.
