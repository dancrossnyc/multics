03/10/76 print_text, pt

Function:  prints one or more lines of a segment.


Syntax:  pt {path} {-control_arg} {first_line} {last_line}


Arguments:
path   pathname of the segment to be printed; if omitted,
temporary text is used.

first_line   line number of the line at which to begin printing.

last_line   line number of the line at which to stop printing.


Control arguments:
-pathname path, -pn path   specifies that path begins
with a digit.
-no_header, -nhe   suppresses the header line.


Notes:  If only the first_line argument is specified, only
that line is printed.  The first line and last line
numbers specified do not have to appear in the segment.  The command
prints all lines that fall within the range of the two numbers.
