03/09/76 dprint, dp

Function:  queues segments for printing on the line printer.


Syntax:  dp {-control_args} {paths}


Arguments:  paths are the pathnames of segments.


Control arguments:  affect only the pathnames that follow them.
-copy N, -cp N  prints N copies (N<=4) of specified paths  (default
is 1).
-queue N, -q N  prints paths in priority queue N (N<=3)  (default
default is 3).
-header XX -he XX identifies output by the string XX  (default is
user's Person_id).
-destination XX -ds XX labels output with the string XX  (default
is user's Project_id).


Notes:  If the command is invoked without any arguments, Multics prints
a message giving the status of queue 3.
