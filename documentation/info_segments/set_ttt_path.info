10/17/84  set_ttt_path

Syntax as a command:  set_ttt_path {path} {-control_arg}


Function:  changes the pathname of the terminal type table (TTT)
associated with your process.


Arguments:
path
   is the pathname of the TTT.  If you don't give it, you must supply
   the control argument.


Control arguments:
-reset, -rs
   resets the TTT pathname to its default value:
   >system_control_1>ttt.
