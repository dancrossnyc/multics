02/15/85  minute

Syntax as a command:  minute {time_string} {-control_arg}


Syntax as an active function:  [minute {time_string} {-control_arg}]


Function: returns the one- or two-digit number of a minute of the
hour, from 0 to 59.  The format string to produce this is "^Z9MH".


Arguments:
time_string
   indicates the minute about which information is desired.  If you
   supply no time_string, the current minute is used.  The time string
   is concatenated to form a single argument even if it contains
   spaces; you need not quote it.  (See time_strings.gi for a
   description of valid time_string values.)


Control arguments:
-zone STR
   STR specifies the zone that is to be used to express the result.
   (Default: the process default)


Notes: Use the print_time_defaults command to display the default
zone.  Use the display_time_info command to display a list of all
acceptable zone values.
