The resource control package (RCP) controls and allocates peripheral resources
by managing the assignment of all storage and unit record devices and any disk
or tape volumes that can be mounted on them, by producing mount messages, and
by checking users' access to use devices and volumes.  This devices include
printers, card readers, card punches, tapes, and user I/O disks.
