03/31/83  suffix

Syntax:  suffix path


Syntax as active function:  [suffix path]


Function:  returns the last component of the entryname (or archive
component name, if an archive component pathname is supplied) portion
of the specified segment.  If that entryname has only one component,
the null string is returned.


Arguments:
path
   is the pathname from which the trailing name component is removed.


Notes:  Since the pathname is returned in quotes, the command processor
treats it as a single argument regardless of special characters in the
name.
