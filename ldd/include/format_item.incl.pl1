dcl	1 format_item	aligned based,
	2 head		unaligned,
	  3 code		bit(9),
	  3 num		bit(9),
	  3 next		bit(18),
	2 count		fixed bin(35),
	2 value(3)	fixed bin(35);

dcl	1 remote_format	aligned based,
	2 head		unaligned,
	  3 code		bit(9),
	  3 num		bit(9),
	  3 next		bit(18),
	2 cnt		fixed bin(35),
	2 address		unaligned,
	  3 class		bit(18),
	  3 offset	bit(18);
