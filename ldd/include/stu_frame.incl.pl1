dcl	1 frame		aligned based,
	2 pointers(0:7)	ptr,
	2 back		ptr,
	2 next		ptr,
	2 return		ptr,
	2 entry		ptr,
	2 operator	ptr,
	2 argptr		ptr,
	2 skip1(2)	fixed bin,
	2 on_unit_info(2)	bit(18) unaligned,
	2 translator_id	bit(18) unaligned,
	2 operator_return	bit(18) unaligned,
	2 display		ptr,
	2 skip2(2)	fixed bin,
	2 linkage		ptr;
