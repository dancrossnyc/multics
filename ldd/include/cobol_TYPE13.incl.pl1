
/* begin include file ... cobol_TYPE13.incl.pl1
/* Last modified on 11/18/76 by ORN */

		/* header */
     2 size fixed bin,
     2 line fixed bin,
     2 column fixed bin,
     2 type fixed bin,     /* cd = 13 */
		/* body */
     2 string_ptr ptr,
     2 prev_rec ptr,
     2 info,
	3 searched bit(1),
	3 duplicate bit(1),
	3 filler1 bit(6),
     2 options,
	3 input bit(1),
	3 output bit(1),
	3 initial bit(1),
     2 def_line fixed bin,
     2 cd_num fixed bin,
     2 cd_seg fixed bin,
     2 cd_off fixed bin(24),
     2 max_redef fixed bin,
     2 mdest fixed bin,
     2 name_size fixed bin,
     2 name char(0 refer(cdtoken.name_size));

/* end include file ... cobol_TYPE13.incl.pl1 */

