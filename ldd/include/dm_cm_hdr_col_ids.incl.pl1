/* BEGIN INCLUDE FILE dm_cm_hdr_col_ids.incl.pl1 */

/* DESCRIPTION:

   Contains element identifiers of some elements in the Header Collection.
   HEADER_COLLECTION_HEADER_ELEMENT_ID is the identifier of the element in
   which the collection_header for the Header Collection is stored (see
   dm_cm_collection_header.incl.pl1).
   CALLER_HEADER_ELEMENT_ID is the identifier of the element in which the
   caller's file header is stored.
   CM_FILE_HEADER_ELEMENT_ID is the identifier of the element in which the
   cm_file_header structure is stored (see dm_cm_file_header.incl.pl1).
*/

/* HISTORY:
Written by Matthew Pierret, 09/24/84.
Modified:
*/

/* format: style2,ind3,ll79 */
     dcl	   (
	   HEADER_COLLECTION_HEADER_ELEMENT_ID
			      init ("000000000001"b3),
	   CM_FILE_HEADER_ELEMENT_ID
			      init ("000000000002"b3),
	   CALLER_HEADER_ELEMENT_ID
			      init ("000000000003"b3)
	   )		      bit (36) aligned int static
			      options (constant);

/* END INCLUDE FILE  dm_cm_hdr_col_ids.incl.pl1 */
