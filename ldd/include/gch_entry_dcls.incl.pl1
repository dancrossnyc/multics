/* --------------- BEGIN include file gch_entry_dcls.incl.pl1 --------------- */

dcl  graphic_chars_$init ext entry,
     graphic_chars_ ext entry (char (*), fixed bin, float bin, float bin, fixed bin (35)) returns (fixed bin (18)),
     graphic_chars_$set_table ext entry (char (*), char (*), fixed bin (35)),
     graphic_chars_$get_table ext entry (char (*), char (*)),
    (graphic_chars_$long,
     graphic_chars_$long_tb) ext entry
    (char (*), fixed bin, float bin, float bin, float bin, float bin, fixed bin (35)) returns (fixed bin (18));

/* ---------------- END include file gch_entry_dcls.incl.pl1 ---------------- */
