/* begin include file cross_ring_attach_data_ */
/* BIM November 1981 */
/*format: style3 */

declare	attach_data_ptr	pointer;
declare	1 attach_data	aligned based (attach_data_ptr),
	  2 atd		char (200) varying,
	  2 open		char (20) varying,
	  2 inner_index	bit (72) aligned,
	  2 target_ring	fixed bin,
	  2 open_mode	fixed bin;

/* End include file cross_ring_attach_data_ */
