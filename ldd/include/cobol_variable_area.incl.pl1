
/* BEGIN INCLUDE FILE ...   cobol_variable_area.incl.pl1 */
/* Last modified June 25, 1974 by GM */

dcl	1 variable_area aligned based(p2),
	  2 decl_vers fixed bin initial(1),
	  2 identifier char(8) aligned,
	  2 flags fixed bin,
	  2 greatest_severity fixed bin,
	  2 root bit(18) unaligned,
	  2 profile bit(18) unaligned,
	  2 map_first bit(18) unaligned,
	  2 map_last bit(18) unaligned,
	  2 segname_relp bit(18) unaligned,
	  2 segname_length bit(18) unaligned;

/* END INCLUDE FILE ...   cobol_variable_area.incl.pl1 */

