
/* begin include file ... cobol_TYPE18.incl.pl1 */
/* Last modified on 11/7/76 by ORN */

		/* header */
		2 size fixed bin,
		2 line fixed bin,
		2 column fixed bin,
		2 type fixed bin,
		/* body */
		2 string_ptr ptr,
		2 prev_rec ptr,
		2 searched bit (1),
		2 duplicate bit (1),
		2 filler1 bit (1),
		2 debug_ind bit (1),
		2 section_name bit (1),
		2 declarative_proc bit (1),
		2 filler2 bit (1),
		2 alterable bit (1),
		2 priority char (2),
		2 sort_range bit (1),
		2 input_range bit (1),
		2 output_range bit (1),
		2 merge_range bit(1),
		2 filler3 bit (5),
		2 section_num fixed bin,
		2 proc_num fixed bin,
		2 def_line fixed bin,
		2 name_size fixed bin,
		2 name char (30);

/* end include file ... cobol_TYPE18.incl.pl1 */

