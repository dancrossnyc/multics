/* BEGIN INCLUDE FILE fort_symtab_parms.incl.pl1

     Read only parameters passed to fort_symbol_table which describe the
     partially built object segment.

Written:	23 August 1979 by C R Davis
*/

	2 link_base_ptr pointer,
	2 link_reloc_base_ptr pointer,
	2 def_reloc_base_ptr pointer,
	2 current_text_offset fixed binary (18),
	2 current_def_offset fixed binary (18),
	2 current_link_offset fixed binary (18),
	2 final_text_offset fixed binary (18),
	2 profile_offset fixed binary (18),
	2 star_symbol_link fixed binary (18),
	2 first_namelist_symbol fixed binary (18);

/* END INCLUDE FILE fort_symtab_parms.incl.pl1 */
