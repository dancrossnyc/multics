dcl	1 ev_node		based,
	2 next		ptr,
	2 name		ptr,
	2 block		ptr,
	2 exp		ptr,
	2 equiv		ptr,
	2 const		fixed bin(18),
	2 first		fixed bin(18),
	2 last		fixed bin(18),
	2 code		fixed bin(18);

dcl	1 ev_equiv	based,
	2 next		ptr,
	2 name		ptr,
	2 code		fixed bin(18);
