dcl	1 array_dope		aligned based,
	2 data			ptr,
	2 original_bounds(2)	fixed bin,
	2 current_bounds(2)		fixed bin;

dcl	1 scalar_symbol		aligned based,
	2 name			char(2) unaligned,
	2 parameter		bit(1) unaligned,
	2 location		bit(17) unaligned;

dcl	1 array_symbol		aligned based,
	2 name			char(1) unaligned,
	2 skip			bit(9) unaligned,
	2 parameter		bit(1) unaligned,
	2 location		bit(17) unaligned,
	2 offset			fixed bin,
	2 bounds(2)		fixed bin(17) unaligned;
