dcl	1 label_array_element	based aligned,
	2 node_type		bit(9) unaligned,
	2 reserved		bit(12) unaligned,
	2 number			fixed bin(14) unaligned,
	2 next			ptr unaligned,
	2 statement		ptr unaligned,
	2 value			fixed bin(17);
