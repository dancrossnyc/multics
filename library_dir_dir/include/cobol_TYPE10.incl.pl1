
/* begin include file ... cobol_TYPE10.incl.pl1
/* Last modified on 01/25/77 by ORN */

	/* header */
	2 size fixed bin,
	2 line fixed bin,
	2 column fixed bin,
	2 type fixed bin,
	/* body */
	2 string_ptr ptr,
	2 prev_rec ptr,
	2 searched bit(1),
	2 duplicate bit(1),
	2 saved bit(1),
	2 debug_ind bit(1),
	2 filler1 bit(3),
	2 subscript bit(1),
	2 def_line fixed bin,
	2 level fixed bin,
	2 seg_num fixed bin,
	2 offset fixed bin(24),
	2 index_no fixed bin,
	2 min fixed bin,
	2 max fixed bin,
	2 struc_length fixed bin,
	2 cswd_seg fixed bin,
	2 cswd_offset fixed bin(24),
	2 name_size fixed bin,
	2 name char(0 refer(index_name.name_size));



/* end include file ... cobol_TYPE10.incl.pl1 */

