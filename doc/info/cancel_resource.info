02/24/84  cancel_resource, cnr

Syntax:  cnr -id reservation_id {-control_arg}


Function:  cancels reservations made with the reserve_resource command
using the reservation identifiers obtainable from the list_resources
command.


Arguments:
reservation_id
   is the reservation identifier of the reservation to be canceled.
   It must be preceded by the -id control argument.


Control arguments:
-priv
   allows a privileged cancellation to be done, such as a cancellation
   of a reservation belonging to another user.  This control argument
   requires access to rcp_sys_.
