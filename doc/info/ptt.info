02/10/88  print_terminal_types, ptt

Syntax as a command:  ptt {terminal_type_name} {-control_arg}


Function:  Displays the names of all currently-defined terminal types
   matching the given terminal type name which may be a starname.  If
   no name is given, all names will be displayed.


Arguments:
terminal_type_name
   name of a specific terminal type.  This may be a starname.  If
   omitted, all terminal type names will be printed.


Control arguments:
-pathname NAME, -pn NAME
   is the pathname of the terminal type table (TTT) to be
   used to find the names of the defined terminal types.
   If it is omitted, the current default TTT is used (as set by
   set_ttt_path).
