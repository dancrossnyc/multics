10/17/84  teco_ssd

Syntax as a command:  teco_ssd path


Function: specifies a directory for teco to search when trying to find
a teco macro to execute.  The directory specified is searched instead
of your directory.


Arguments:
path
   is the absolute pathname of a directory to be searched by
   teco_get_macro_ instead of your home directory.
